# 裁决与标准变更记录（ADR风格，追加式）

> 格式：`## 日期 | 主题` → 背景 / 决策 / 后果。

## 2026-09-05 | 多子改判SM
- 背景：小黑屋观察位，暂标F待复核；用户已确定其性别为男。
- 决策：male + soft_male（conf=high）。
- 后果：账本 F63/M130；多子286条真实语料归SM侧。

## 2026-09-05 | 前台小姐落F（conf=medium）
- 背景：名片无性别/空间锁；证据链=他称女称8净男称0+被物化方向+游戏浓度1.2%女参照带+咱喵软腔+学生；交叉判定后用户拍板。
- 决策：female，暂不开训练。
- 后果：清梦同款+1（与U1643/ばかか构成3人组，但s0v57/58已证不足训练，待第5个同款）。

## 2026-09-05 | abstain不确定拒绝空间转正
- 背景：三步决策链（塞回train→LLM合成→abstain）前两步阴性。
- 决策：单边带 0.35<P_female<0.5 → 输出"不确定(疑似男域原生女→人工)"；用户确认该带恰好=最早二次元/庭院批女性。
- 后果：F判对96.7%，9.5%人工复核率；待落入推理管线。

## 2026-09-05 | C4双门槛标准（此前固化的SM定义）
- 决策：SM := male ∧ [A: 软(qwr≥0.05|p_llm≥0.5)∧物化<0.05 | B: 软∧物化≥0.10∧女称≥0.03]；灰区mate0.05-0.10默认M待人工。
- 后果：C4落库10人+灰区3人按推荐；后续所有SM判定沿用。

## 2026-09-04 | 二元裁决指标固化
- 决策：SM在acc层归入M——女性acc=真F判F，男性acc(含SM)=真M/SM判非F。
- 后果：train_bert3.py print行[二元裁决]；所有轮次以此为准。


## ADR-2026-09-05c: abstain 规则落推理管线（执行）

- **裁决层**（docs/decisions.md abstain 转正 ADR 的代码化）：
  - `P_f(r3 三seed均值) >= 0.50` → female（自动）
  - `P_f < 0.35` → male（自动，含 SM 侧）
  - `0.35 <= P_f < 0.50` → **abstain**（不确定：疑似男域原生女 → 转人工/带外验证）
- **交付物**：
  - `train/infer_r3.py`：r3 三 seed 集成实时推理服务（stdin/stdout JSON 协议），带 verdict/band 输出；实测清梦 0.373→abstain、白雪易融 0.501→female、酒神爵 0.353→abstain（12M 误捞带内符合预期）、Buchi 0.519→female、ばかか 0.315→male
  - `outputs/r3-abstain-scores.csv`：r3-s0v56 三 seed 全账本静态表（191 人，源 staging/abstain-eval-s0v56.csv）
  - `src/infer.js`：buildReply 加 r3 abstain 层——静态表命中的用户按 r3 裁决输出"不确定（疑似男域原生女）"，未命中走原 v10-wb 老逻辑（向后兼容）
- **Pi 部署**（2026-09-05 18:07）：4 个 collector 重启生效；原版备份 `/opt/qqbot/collector/src/infer.js.bak-preabstain-20260905`；重启后 139/263/292 WS 全部重连正常
- **注意**：v10-wb 与 r3 的 P 值标定不可换算（清梦 v10=0.98 / r3=0.37），abstain 带只对 r3 口径有效；274 的 WS 3001 报错系 llbot-274 dead 遗留，与部署无关


## ADR-2026-09-05d: 昵称通道证伪，s0v56+abstain 维持现役

s0v59a 消融（唯一差异 --use-nickname）结果：val 用户级 acc 0.78-0.83→0.50-0.56，female val acc→0.25-0.375；全账本预测饱和化，带 18→1 属假性收窄——清梦 0.367→0.004 自信判男（红线），原判对 F 翻转 5 人。**昵称是捷径泄漏通道而非风格表征**，r3 线关闭昵称的原设计正确。裁决：s0v59 归档不入役；拒绝区提升回归两条已批路线——M 侧降噪三件套（改名玩梗时效过滤/模板去重/错因诊断）+ 清梦同款 5+ 后的多原型轮；context 通道变体降级 backlog（导出器语义已核实支持含他人昵称的上下文，理论可达但预期需按两连阴性管理）。


## ADR-2026-09-05e: 小黑屋首例F标注 + 第三参考通道上线

- **标注**：U2240（Kay，小黑屋群 G029）= female / manual-user-confirmed / high（用户口令）。账本 F 63→64。小黑屋群首例标注
- **第三参考通道**（用户设计：排除带外的标定模型）：
  - 实现：带外 172 人（F55/M117）拟合逻辑回归（类平衡，特征 = p_r3 + p_v10 + 语料量/长度/表情密度 6 维），5 折 CV acc 0.983 / AUC 0.999（⚠️置信区数字，对带内仅诊断参考），全量重拟合后输出带内校准 P(女)
  - 带内诊断：Top6 中真 F 4/6（清梦 0.605/白雪易融 0.949/U769 0.736/U2071 0.632 在列），U1990(M) 0.709 与 U2423(M) 0.685 误偏高，ばかか(F) 0.048 被压底——**与真值相关但不足以单飞**
  - 三通道参考矩阵（v10 排序 / LLM 倾向 / 标定 P）落盘 `outputs/band-reference-s0v56.csv`（无真值列防泄漏）：共识 F（≥2 票）3 人 = 真 F2（白雪易融+清梦，三通道全一致）+ 真 M1（U2423）；共识 M 8 人 = 真 M7 + ばかか(F)1；无信号留观 2 人
  - **裁决口径（沿用 ADR-2026-09-05c）**：三通道仅作带内人工复核的参考信号，不改变 0.35-0.50 abstain 裁决边界
- **线上部署**（22:52）：infer.js abstain 分支追加"参考信号"行（v10/LLM/标定P/F票M票），PC+Pi 同步，备份 infer.js.bak-preref-20260905，4 服务重启全部 active、139 WS 重连正常
